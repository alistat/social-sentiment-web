{
  "name": "social-sentiment-web",
  "version": "0.0.0",
  "description": "Sentiment analysis from social media, Web frontend",
  "author": "irini",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.0.0",
    "cross-env": "^5.2.0",
    "element-ui": "^2.4.6",
    "express": "^4.16.3",
    "nuxt": "^2.0.0",
    "pug": "^2.0.3",
    "scss": "^0.2.4"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
